# معمارية لوحة إدارة قطوف الطبيعة

## الهدف

تهدف هذه المعمارية إلى تحويل الوظائف الإدارية المتفرقة في متجر قطوف الطبيعة إلى مركز تشغيل موحّد يخدم المالك وفريق خدمة العملاء ومسؤولي المخزون والموردين والموصلين، مع الحفاظ على متجر العملاء الحالي، ونظام الدخول الحالي، وتعديلات واتساب والإعدادات اليمنية.

القاعدة الأساسية هي أن لوحة الإدارة ستكون **طبقة تشغيل فوق النظام القائم** وليست نظامًا موازيًا. ولذلك ستُعاد الاستفادة من الكيانات الحالية مثل `User` و`Role` و`Permission` و`Customer` و`Order` و`Supplier` و`DeliveryDriver` و`DeliveryTracking` و`AuditLog`، مع إضافة ما ينقص فقط.

## مبادئ عدم الكسر

لن نغير واجهات API الحالية أو أسماء الحقول المستعملة في المتجر إلا عند الحاجة إلى توافق خلفي واضح. أي تغيير في قاعدة البيانات سيبدأ بإضافة حقول اختيارية أو جداول جديدة، ثم ترحيل البيانات، ثم تفعيل القراءة الجديدة، ثم تفعيل الكتابة الجديدة؛ ولن نحذف حقلًا قديمًا قبل التأكد من عدم استخدامه في الواجهة أو الخلفية أو بيانات الإنتاج.

ستبقى تعديلات واتساب وبيانات المتجر الحالية خارج نطاق إعادة الهيكلة. كما سيتم فصل الملفات المصدرية عن الملفات المولدة من bundling، ولن يُنفذ `reset` أو `checkout` إجباري على العمل المحلي.

## نموذج فريق العمل

| الدور التشغيلي | الدور التقني الحالي | نطاق الوصول المقترح |
|---|---|---|
| مالك المتجر عمار عادل | `SUPER_ADMIN` | جميع المستأجرات والأنظمة والتقارير وسجل التدقيق |
| مدير المتجر | `ADMIN` | الإدارة التشغيلية، الفريق، المنتجات، الطلبات، الموردون، التقارير، دون تغيير أسرار البنية التحتية |
| مدير العمليات | `MANAGER` | الطلبات والعملاء والمخزون والموصلون والتقارير التشغيلية |
| موظف خدمة العملاء | `EMPLOYEE` | العملاء والطلبات والتذاكر والملاحظات، مع منع حذف البيانات أو تعديل الصلاحيات |
| الموصل | حساب تشغيلي محدود | الطلبات المسندة إليه وحالة التوصيل فقط |
| المورد | حساب خارجي محدود عند الحاجة | التوريدات وأوامر الشراء الخاصة به فقط |
| العميل | `CUSTOMER` | ملفه وطلباته وعناوينه وسلة الشراء |

لن نضيف اسم `STAFF` إلى قاعدة البيانات الآن. بدل ذلك سنعالج التوافق بين `STAFF` الظاهر في بعض أجزاء الواجهة و`EMPLOYEE` المعتمد في RBAC المركزي عبر طبقة تطبيع واحدة، ثم نمنع استخدام مقارنات أدوار متفرقة داخل الصفحات الجديدة.

## الصلاحيات الجديدة المطلوبة

سيتم توسيع سجل الصلاحيات تدريجيًا، مع إبقاء الصيغة الحالية `module:action` حتى لا تتأثر `ProtectedRoute` أو الواجهات الموجودة.

| الوحدة | أمثلة الصلاحيات |
|---|---|
| العملاء | `customers:list`, `customers:read`, `customers:create`, `customers:update`, `customers:delete` |
| الطلبات | `orders:list`, `orders:read`, `orders:update`, `orders:delete` |
| خدمة العملاء | `support:list`, `support:read`, `support:create`, `support:update`, `support:assign` |
| الموصلون | `drivers:list`, `drivers:read`, `drivers:create`, `drivers:update`, `drivers:assign` |
| الموردون | الصلاحيات الحالية `suppliers:*` مع ربطها بالمشتريات والمخزون |
| التقارير | `reports:list`, `reports:read`, `reports:export` |
| التدقيق | `audit:list`, `audit:read` |

إذا كان إدخال وحدات `support` أو `drivers` في النوع الحالي يحتاج تعديلًا واسعًا، فسنستخدم أولًا أقرب وحدات موجودة مع إضافة الأنواع بشكل متوافق، ثم نضيف الاختبارات قبل تفعيل المسارات.

## مركز العملاء

سيعرض مركز العملاء بيانات الهوية والتواصل، عدد الطلبات، آخر طلب، إجمالي الإنفاق، متوسط قيمة الطلب، حالة الحساب، العناوين، الملاحظات، وسجل التفاعل. ستدعم القائمة البحث برقم الهاتف أو الاسم أو البريد أو رمز العميل، مع pagination وخيارات فرز واضحة.

### تصنيف العملاء بشكل مهني

عبارة «من يريد اللعب» لا تصلح كحكم تلقائي على شخص. لذلك سنحوّلها إلى تصنيف قابل للشرح والمراجعة. سيكون لكل عميل حالة تشغيلية مثل **جديد، نشط، متكرر، يحتاج متابعة، عالي المخاطر، محظور**، مع سبب وتاريخ واسم الموظف الذي أضاف التصنيف.

لن يُرفع مستوى المخاطر بسبب طلب واحد أو انطباع موظف. ستعتمد الإشارات على وقائع قابلة للتدقيق، مثل تكرار الإلغاء بعد التجهيز، أرقام غير قابلة للتواصل، طلبات مكررة غير مكتملة، أو نزاع مسجل. ويظل التصنيف قابلًا للتعديل اليدوي مع تسجيل السبب في `AuditLog`.

## دورة الطلب والتوصيل

سيظل `OrderStatus` الحالي هو مصدر الحالة الأساسي حتى لا تنكسر واجهات العميل. سنستخدم خريطة انتقالات مركزية:

| المرحلة | الانتقالات المسموحة افتراضيًا |
|---|---|
| `DRAFT` | `PENDING`, `CANCELED` |
| `PENDING` | `CONFIRMED`, `CANCELED` |
| `CONFIRMED` | `PACKED`, `CANCELED` |
| `PACKED` | `SHIPPED`, `CANCELED` |
| `SHIPPED` | `DELIVERED`, `RETURNED` |
| `DELIVERED` | `RETURNED` عند وجود سبب موثق |
| `CANCELED`, `RETURNED`, `REFUNDED` | حالات نهائية، ولا تُعاد دون صلاحية مدير وسبب واضح |

أما حالة التوصيل في `DeliveryTracking` فستبقى منفصلة: `PENDING`, `ASSIGNED`, `IN_TRANSIT`, `DELIVERED`, `FAILED`, `RETURNED`. لا يجوز تعيين الموصل أو تغيير حالة التوصيل من الواجهة فقط؛ يجب أن يتحقق الخادم من الصلاحية، ملكية الطلب، وصحة الانتقال.

## التغييرات المقترحة في البيانات

| الكيان | الوضع الحالي | التغيير الآمن المقترح |
|---|---|---|
| `Customer` | يحتوي الحالة والملاحظات الأساسية | إضافة حقول اختيارية للتصنيف وسبب التصنيف وآخر تواصل عند الحاجة |
| سجل التواصل | غير ظاهر في المخطط الحالي | جدول `CustomerInteraction` مرتبط بالعميل والموظف والطلب، مع القناة والنتيجة والملاحظة |
| الطلب | يحتوي الحالة والتوصيل والدفع | إضافة سجل تاريخ حالات `OrderStatusHistory` بدل الاعتماد على الحالة الحالية فقط |
| التوصيل | يحتوي الموصل والحالة ووقت البدء والتسليم | إضافة ملاحظات الفشل وإثبات التسليم ووقت آخر تحديث بشكل اختياري |
| الموصل | يحتوي الاسم والهاتف ومعلومات المركبة | إضافة حالة النشاط ومناطق العمل وربط اختياري بحساب `User` |
| التذاكر | غير موجودة ككيان واضح | جدول `SupportTicket` مع الحالة والأولوية والتعيين والطلب والعميل |
| التدقيق | `AuditLog` موجود | إعادة استخدامه لكل عمليات التصنيف وتغيير الحالة والتعيين والتعديل الحساس |

لن نضيف هذه الجداول كلها دفعة واحدة. الدفعة الأولى ستقتصر على العقود والصلاحيات وصفحة الإدارة والقراءة الآمنة، ثم تضاف التفاعلات وسجل الحالات في دفعات منفصلة.

## بنية الواجهة

سنضيف مسارًا محميًا `/admin` داخل `AppShell`، مع `DashboardLayout` أو مكوّن shell الإدارة القائم إن كان متوافقًا مع البنية الحالية. ستكون لوحة الإدارة ذات sidebar ثابت على سطح المكتب، وdrawer عملي على الهاتف، وتحتوي على:

| القسم | المسارات المقترحة |
|---|---|
| نظرة عامة | `/admin` |
| الطلبات | `/admin/orders`, `/admin/orders/:id` |
| العملاء | `/admin/customers`, `/admin/customers/:id` |
| خدمة العملاء | `/admin/support`, `/admin/support/:id` |
| الموصلون | `/admin/drivers`, `/admin/drivers/:id` |
| الموردون | `/admin/suppliers` مع إعادة استخدام صفحات الموردين الحالية |
| المخزون | `/admin/inventory` مع إعادة استخدام الصفحات الحالية |
| الفريق والصلاحيات | `/admin/users`, `/admin/roles` |
| التقارير والتدقيق | `/admin/reports`, `/admin/audit` |

لن تُعرض عناصر التنقل إلا بعد التحقق من الصلاحية، لكن حماية الخادم ستظل إلزامية حتى لو تم تجاوز الواجهة يدويًا.

## ترتيب التنفيذ

سنبدأ بإنشاء طبقة تعريف موحدة للصلاحيات وتطبيع الأدوار، ثم نضيف endpoint قراءة لوحة الإدارة، ثم صفحة `/admin`، ثم مركز العملاء والطلبات. بعد نجاح هذه المجموعة في TypeScript واختبارات المسارات، نضيف التذاكر والموصلين وسجل الحالات، ثم الموردين والمخزون والتقارير.

كل دفعة ستحتوي على ثلاثة مخرجات: كود المصدر، اختبار أو فحص قابل للتكرار، وتوثيق مختصر يوضح الملفات المتأثرة وطريقة التراجع. ولن يتم اعتبار الدفعة جاهزة للنشر لمجرد أن الصفحة تظهر؛ يجب نجاح build، والتحقق من الصلاحيات، وفحص الهاتف، والتحقق من عدم تأثر تسجيل الدخول والمتجر العام.

## معايير القبول

تُقبل المرحلة الأولى عندما يستطيع المدير المصرح له دخول `/admin`، ويرى مؤشرات حقيقية من قاعدة البيانات، ولا يستطيع العميل العادي فتح أي مسار إداري، ولا يستطيع موظف خدمة العملاء تعديل الصلاحيات أو الإعدادات الحساسة، وتظهر حالات التحميل والفراغ والخطأ بشكل واضح، وتبقى الصفحات العامة والدفع وواتساب عاملة كما كانت.

## ملاحظات أمنية من تدقيق المسارات الحالية

أظهر التدقيق أن مسارات تذاكر الدعم الحالية تتطلب تسجيل الدخول، لكن خيارات `authorizationRequired` و`requiredPermissions` ليست محددة على مستوى المسار. بعض القراءة والرد يعتمد على فحص الدور داخل controller، بينما تحديث حالة التذكرة لا يفرض في controller الحالي صلاحية موظف أو مدير واضحة. لذلك لن نربط لوحة الإدارة بهذه العملية كما هي؛ ستُضاف طبقة تفويض صريحة على مستوى route/controller، مع حصر التذاكر التي يستطيع العميل قراءتها أو الرد عليها، وحصر تغيير الحالة على فريق خدمة العملاء والمديرين.

كما يجب أن يستخدم تحديد الصلاحيات `permissions` الفعلية لا اسم الدور وحده، مع اعتبار الدور طبقة عرض أو fallback فقط. هذا يمنع أن يحصل مستخدم على صلاحية حساسة بسبب مقارنة نصية غير موحدة بين `ADMIN` و`MANAGER` و`EMPLOYEE` و`STAFF`.
