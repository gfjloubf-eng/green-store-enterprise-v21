# مراجع تحسين الصور والكاش

## web.dev — Image performance
الرابط: https://web.dev/learn/performance/image-performance

الصور غالباً من أثقل الموارد. التوصيات الرسمية تشمل إرسال أبعاد مناسبة للجهاز، استخدام `srcset` و`sizes`، وتفضيل WebP أو AVIF عند توفر ملفات مناسبة. يجب إبقاء التحميل الكسول للصور غير الظاهرة، مع عدم تأخير الصورة الرئيسية الظاهرة.

## MDN — HTTP caching
الرابط: https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/Caching

تعذر فتح المصدر عبر المتصفح بسبب مهلة اتصال، لكن سياسة عامة آمنة هي استخدام كاش طويل للملفات ذات أسماء versioned/hashed، وإعادة التحقق للبيانات المتغيرة، وعدم جعل الأسعار والمخزون النهائيين يعتمدون على نسخة قديمة.

## تطبيق آمن على قطوف

الأولوية هي `loading=lazy` و`decoding=async`، ثم `srcset/sizes` فقط إذا كانت نسخ الصور موجودة فعلاً، ثم كاش الكتالوج لمدة قصيرة مع تحديث صامت. لا نضيف Service Worker شامل قبل اختبار منفصل، ولا نغير روابط قاعدة البيانات أو Supabase Storage عشوائياً.
