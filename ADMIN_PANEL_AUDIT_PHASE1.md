# تدقيق لوحة الإدارة — المرحلة الأولى

## نطاق التدقيق
تمت مراجعة حالة المستودع المحلي، حزمة الواجهة، حزمة الخلفية، مخطط Prisma، توجيه الواجهة، AuthProvider، إعدادات التنقل، وسجل RBAC.

## حالة المستودع
- الفرع المحلي `main` متقدم 3 commits عن `origin/main`.
- توجد 12 ملفات معدلة محليًا و3 ملفات غير متتبعة.
- التعديلات المحلية تتضمن نافذة قنوات واتساب، بيانات المتجر اليمنية، وتطبيع إعدادات قديمة.
- لا يجوز البدء بتعديل ملفات هذه التعديلات دون حفظها أو فصلها عن دفعة لوحة الإدارة.
- يجب عدم تنفيذ reset أو checkout إجباري.

## البنية الحالية
- الواجهة React 19 + Vite + TypeScript.
- الخلفية المضمّنة للنشر تحت `frontend-react/api` وتستخدم Node/Prisma/PostgreSQL.
- توجد أيضًا طبقة خلفية مستقلة تحت `backend`.
- مخطط Prisma غني ويضم Tenant/Store/Branch/User/Role/Permission/Customer/Supplier/Order/DeliveryDriver/DeliveryTracking/Inventory/AuditLog وغيرها.
- توجد مسارات إدارية قائمة للمستخدمين والأدوار والمنتجات والمخزون والموردين والمشتريات.

## نقاط قوة قابلة لإعادة الاستخدام
- User/Role/Permission/RolePermission/UserRole موجودة.
- أدوار RBAC الحالية: SUPER_ADMIN, ADMIN, MANAGER, EMPLOYEE, CUSTOMER.
- سجل AuditLog وActivityLog وSecurityLog موجود.
- Customer وOrder وSupplier وDeliveryDriver وDeliveryTracking موجودة في المخطط.
- ProtectedRoute موجود ويعتمد على hasPermission/hasRole.
- navigation.ts هو نقطة الإدراج الصحيحة للتنقل.

## مخاطر يجب علاجها قبل التوسع
1. تعارض تسمية محتمل بين `EMPLOYEE` في RBAC الخلفي و`STAFF` في بعض منطق الواجهة/عميل المصادقة.
2. بعض مسارات ربط الأدوار والصلاحيات تحتاج تدقيقًا إضافيًا للتأكد من وجود `requiredPermissions` صريحة.
3. OrderStatus وDeliveryStatus موجودان لكن دورة التشغيل المطلوبة لخدمة العملاء تحتاج خريطة انتقالات موحدة وسجل تغييرات.
4. Supplier وDeliveryDriver موجودان، لكن لا يظهر من المخطط وحده وجود سجل تواصل/تذاكر خدمة عملاء أو مؤشرات عميل موثوقة؛ يجب عدم اختراع تصنيف «يريد اللعب» قبل وضع تعريف تشغيلي قابل للتدقيق.
5. العمل المحلي غير المرفوع قد يحتوي ناتج bundling، لذلك يجب فصل ملفات المصدر عن الملفات المولدة قبل البناء النهائي.
6. إعدادات النشر وقاعدة البيانات تحتاج تحققًا آمنًا، ولا يجب وضع أسرار أو كلمات مرور في المستودع.

## قرار هندسي أولي
- لا نعيد كتابة النظام.
- نبدأ بتثبيت baseline: build/lint وفحص TypeScript، ثم نوثق النتائج.
- نضيف لوحة الإدارة على طبقات: عقد صلاحيات، endpoints، صفحات، اختبارات، ثم نشر تدريجي.
- نستخدم نموذج تصنيف عملاء مهنيًا مثل: موثوق، جديد، يحتاج متابعة، عالي المخاطر، بدل وصف اتهامي غير قابل للقياس.
- كل دفعة يجب أن تكون قابلة للرجوع، مع عدم المساس بتعديلات واتساب والإعدادات الحالية.

## نتائج baseline

- فحص TypeScript للواجهة عبر `tsc -b --noEmit` ناجح برمز خروج 0.
- فحص ESLint يفشل بسبب عدد كبير من المخالفات الموجودة مسبقًا، ومنها `no-explicit-any` و`no-empty-object-type` و`no-unused-vars` في طبقات backend المضمنة؛ لن نخلط إصلاحها جميعًا مع لوحة الإدارة، لكن سنمنع إضافة مخالفات جديدة في الملفات الجديدة.
- الخلفية المستقلة لا تحتوي `node_modules/.bin/tsc` محليًا، لذلك لم يمكن بناءها من هذه البيئة دون تثبيت dependencies. لم يتم تنفيذ تثبيت أو تغيير في الملفات.
- النتيجة: الواجهة قابلة للترجمة TypeScript حاليًا، لكن جودة lint العامة تحتاج مسار تنظيف مستقل لاحقًا.

## تقدم المرحلة الرابعة — العملاء وخدمة العملاء

- تمت إضافة مركز إدارة محمي على المسار `/admin` للأدوار `SUPER_ADMIN` و`ADMIN` و`MANAGER` و`EMPLOYEE`.
- تمت إضافة دعم مركزي لـ `requiredRoles` في أنواع التنقل وفلترة Sidebar.
- تمت إضافة `/admin/customers` و`/admin/customers/:customerId` باستخدام عقود خدمات العملاء والطلبات الموجودة.
- تمت إضافة `/admin/support` باستخدام API تذاكر الدعم الحالي، مع القائمة والتصفية والرد وتغيير الحالة.
- تم تشديد تغيير حالة تذكرة الدعم: لا يسمح به إلا لـ `SUPER_ADMIN` أو `ADMIN` أو `MANAGER` أو `EMPLOYEE`، مع قبول `STAFF` القديم داخل فحص التوافق في controller.
- تمت إضافة حماية أدوار على مستوى المسار `PATCH /support/tickets/:id/status`.
- نتيجة TypeScript للواجهة بعد التعديلات: ناجحة (`typescript_exit=0`).
- لم تتم محاولة نشر إنتاجي بسبب حد Vercel المجاني الموروث من الجلسة السابقة.

## دفعة 2026-08-19 — الموردون والموصلون

- أضيفت شاشة `AdminDriversPage` بمسار `/admin/drivers` محمي لأدوار الإدارة، مع قائمة الموصلين، البحث، الإضافة، التعديل، الحذف الناعم، وإحصاءات عمليات التوصيل.
- أضيفت شاشة `AdminSuppliersPage` بمسار `/admin/suppliers` محمي لأدوار الإدارة، مع قائمة الموردين، البحث، الإضافة، التعديل، الحذف الناعم، وإحصاءات جهات الاتصال وأوامر الشراء.
- أضيفت `deliveryClient.ts` و`supplierAdminClient.ts` بعقود واجهة موحدة ومعالجة أخطاء ومصادقة تلقائية.
- أضيفت وحدة API للموصلين في backend ومزامنتها مع النسخة المضمنة، مع صلاحيات `delivery:read/create/update/delete` وعزل tenant.
- أضيفت وحدة API إدارة الموردين في backend ومزامنتها مع النسخة المضمنة، مع صلاحيات `suppliers:read/create/update/delete` وعزل tenant.
- TypeScript: ناجح (`exit 0`).
- esbuild لحزمة API: ناجح (`exit 0`).
- Vite production build: ناجح (`exit 0`).
- يوجد تحذير Vite سابق متعلق بحجم chunk الرئيسي (~584 kB) وتحذير ineffective dynamic import لـ cartClient؛ لا يمنع البناء لكنه سيدخل ضمن تحسين الأداء في مرحلة الاختبارات.
- لم يتم نشر أي تغيير إلى Vercel في هذه الدفعة.

## بوابة الجودة النهائية — 2026-08-19

- فحص TypeScript للواجهة: ناجح، `exit 0`.
- فحص ESLint للملفات الجديدة في لوحة الإدارة وعملاء الموصلين والموردين: ناجح، `exit 0` بعد إزالة `any` من عملاء API الجديدة وتوثيق effects الخاصة بتحميل البيانات.
- الفحص الدخاني `scripts/admin_smoke_check.mjs`: ناجح، `ADMIN_SMOKE_PASS checks=11`.
- `git diff --check`: ناجح، دون أخطاء تنسيق في الفروقات.
- بناء Vite الإنتاجي: ناجح، مع تحذيرات غير حاجبة تخص حجم chunk كبيرًا واستيرادًا ديناميكيًا موجودًا مسبقًا.
- فحص ESLint الكامل للمشروع ما زال يعرض مخالفات baseline قديمة خارج نطاق الدفعة الحالية؛ لم تُخفَ هذه النتيجة ولم تُخلط مع نتيجة الملفات الجديدة.
- لم يتم نشر هذه الدفعة إلى Vercel بسبب قيد النشر المجاني الموروث. يجب إجراء نشر تدريجي بعد عودة الحد، ثم فحص `/api/health` ومسارات الإدارة بحساب إداري حقيقي.

## تعليمات التشغيل بعد النشر

1. تسجيل الدخول بحساب مالك المتجر أو مدير مصرح له، ثم فتح `/admin`.
2. اختبار مركز العملاء والطلبات وخدمة العملاء والموردين والموصلين والتقارير على بيانات فعلية.
3. التحقق من منع حساب العميل العادي من رؤية روابط الإدارة أو استدعاء المسارات الإدارية.
4. التحقق من ظهور الريال اليمني وبيانات المتجر اليمنية في المواضع العامة والتقارير.
5. عدم تغيير مخطط قاعدة البيانات الإنتاجية قبل نسخة احتياطية وتجربة migration على بيئة منفصلة.
6. تغيير كلمة مرور Supabase التي ظهرت سابقًا في المحادثة قبل أي نشر إنتاجي جديد.

## ملفات التسليم المهمة

- `ADMIN_PANEL_ARCHITECTURE.md`: المعمارية ونموذج الأدوار والبيانات.
- `ADMIN_PANEL_AUDIT_PHASE1.md`: سجل التدقيق ونتائج الاختبارات.
- `scripts/admin_smoke_check.mjs`: فحص دخاني قابل لإعادة التشغيل.
- `frontend-react/src/features/admin/pages/`: صفحات مركز الإدارة والعملاء والطلبات والدعم والموردين والموصلين.
- `frontend-react/src/services/deliveryClient.ts` و`frontend-react/src/services/supplierAdminClient.ts`: عملاء API الجديدة.
