/* ============================================================
   GSDS v1.1 — State Layer Barrel Export
   Green Store Design System — Enterprise UI Foundation
   Milestone 4.3
   ============================================================ */

export type { ProductState, ProductListState, ProductDetailState } from './productState';
export {
  loadingState,
  successState,
  emptyState,
  errorState,
  readyState,
  isState,
  getData,
  getErrorMessage,
} from './productState';
